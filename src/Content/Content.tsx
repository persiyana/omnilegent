import React, { FC, ReactElement } from "react";
import { Box, Container, Grid, Typography, autocompleteClasses } from "@mui/material";

export const Content: FC = (): ReactElement => {
  return (
   <Box
    m="auto"
        sx={{
            width: "75%",
            minHeight: "80%",
            backgroundColor: "#F5F5F5",
            opacity: [0.9, 0.8, 0.7],
            
            
        }} 
    >
    </Box>
    
    
  );
}

